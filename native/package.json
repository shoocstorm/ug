{
  "name": "ultragraph",
  "version": "0.1.0",
  "description": "High-Performance Knowledge Base Indexer",
  "main": "index.js",
  "napi": {
    "binaryName": "ultragraph",
    "platforms": [
      "aarch64-apple-darwin"
    ]
  },
  "scripts": {
    "build": "cargo build --release && mkdir -p ../ugout && cp target/release/ug ../ugout/ug",
    "postbuild": "napi build --output-dir ../ugout",
    "build:dev": "cargo build --profile dev-fast && mkdir -p ../ugout && cp target/dev-fast/ug ../ugout/ug",
    "postbuild:dev": "napi build --output-dir ../ugout",
    "test": "cargo test -- --nocapture"
  }
}